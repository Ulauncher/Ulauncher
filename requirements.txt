# Keep all .venv depdencencies in this file.
# It's used as the source of truth to determine if a .venv is outdated
#
# NOTE: pygobject-stubs needs PYGOBJECT_STUB_CONFIG=Gtk3,Gdk3,Soup2

build
git-cliff >= 2.13, < 3
lefthook == 2.*
pygobject-stubs==2.12.0
pyrefly == 1.0.*  # wait for #3867 to be fixed before upgrading: https://github.com/facebook/pyrefly/issues/3867
pytest
pytest-mock
ruff >= 0.15.17, < 0.16.0
rumdl == 0.2.*; python_version >= '3.9'
typing-extensions
typos == 1.*
